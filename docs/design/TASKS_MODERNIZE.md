| ID | Title | Description | Files | Estimate | Owner | Acceptance Criteria |
|---|---|---|---|---:|---|---|
| M-1 | Add tokens JSON and CSS variables | Create the MODERNIZE token source of truth and static CSS variable mapping. | `tokens/modernize-tokens.json`, `docs/design/reference/tokens.css` | 1h | You | JSON parses cleanly and CSS variables match token names. |
| M-2 | Add Hero component reference | Build the hero reference component with LCP-aware markup and clear CTAs. | `docs/design/reference/components/Hero.tsx` | 2h | You | Component renders in a stock Next.js app and names the hero as the LCP target. |
| M-3 | Add supporting component stubs | Create Card, ProjectLayout, CodeBlock, CTA, Nav, and Footer references. | `docs/design/reference/components/*.tsx` | 4h | You | Components compile with minimal edits and each includes a short `// WHY` comment. |
| M-4 | Add interactive demo references | Create MediaGallery and DemoRunner references with keyboard-safe behavior. | `docs/design/reference/components/*.tsx` | 3h | You | Both components work without hidden services or secrets. |
| M-5 | Build example pages | Create homepage and project page references that map the recommended mode into App Router page examples. | `docs/design/reference/app/home/page.tsx`, `docs/design/reference/app/project/page.tsx` | 3h | You | Both pages render and use the reference components consistently. |
| M-6 | Produce deterministic SVG mockups | Create 3 pages x 3 breakpoints of SVG fixtures for review and screenshot export. | `docs/design/mockups/*.svg` | 6h | You | All mockups open locally and filenames/alt text match the package. |
| M-7 | Draft rollout and PR artifacts | Create the PR body, backlog, and canary rollout notes. | `docs/design/PR_BODY_MODERNIZE.md`, `docs/design/TASKS_MODERNIZE.md`, `docs/visual-upgrade-package.md` | 2h | You | A reviewer can understand the package and rollout path without extra explanation. |
| M-8 | Run QA and repo safety checks | Parse JSON, inspect docs, and verify no secrets or internal data appear in generated artifacts. | docs + tokens | 2h | You | JSON parses, files read cleanly, and no sanitization issues are found. |
