# Sigma Integration Notes

Use this lane for portable detection logic:
- `rules/` stores raw Sigma YAML
- `mappings/` documents how Sigma logic is implemented in Wazuh rules

SPL queries can be referenced for logic parity, but SPL is not directly executable in Wazuh.

