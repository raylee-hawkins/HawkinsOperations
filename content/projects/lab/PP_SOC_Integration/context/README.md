# Context Asset Index

This folder intentionally stores only lightweight index content.

Canonical project context assets live in:
- `site/assets/pp_soc_integration/t5-proxmox-redacted.png`
- `site/assets/pp_soc_integration/t5-infrastructure-map.png`
- `site/assets/pp_soc_integration/t3-workflow.png`

LinkedIn transition post export:
- Archived offline (not stored in this repository) to keep public git history lightweight.

Sanitization posture:
- Do not publish host-specific paths such as `/home/<user>`, `C:\Users\<user>`, or `/srv/<service>`.
- Use placeholders such as `[REDACTED_USER]`, `%USERPROFILE%`, and `[REDACTED_SERVICE]` in recruiter-facing artifacts.
